{
  "name": "recipe-ai",
  "type": "module",
  "version": "0.1.0",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build && esbuild src/background.ts --bundle --outfile=dist/background.js && esbuild src/utils/readability-wrapper.ts --bundle --outfile=dist/Readability.js",
    "preview": "astro preview",
    "astro": "astro",
    "build:watch": "astro build --watch",
    "test": "pnpm run build && NODE_OPTIONS=--no-deprecation web-ext run -s dist",
    "test:firefox": "pnpm run build && NODE_OPTIONS=--no-deprecation web-ext run -t firefox-desktop -s dist",
    "test:chromium": "pnpm run build && NODE_OPTIONS=--no-deprecation web-ext run -t chromium -s dist"
  },
  "dependencies": {
    "@mozilla/readability": "^0.6.0",
    "@xenova/transformers": "^2.17.2",
    "astro": "^5.17.1"
  },
  "devDependencies": {
    "@types/chrome": "^0.1.37",
    "@types/mozilla-readability": "^0.2.1",
    "esbuild": "^0.27.3",
    "rollup-plugin-copy": "^3.5.0",
    "sass": "^1.97.3",
    "web-ext": "^9.3.0"
  }
}